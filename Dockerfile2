# Nginx의 경량 버전인 Alpine 이미지를 사용
FROM nginx:alpine

# /test 디렉토리를 Nginx 기본 HTML 디렉토리 (/usr/share/nginx/html)로 복사
COPY /test /usr/share/nginx/html

# 로컬의 nginx.conf 파일을 Nginx의 기본 설정 파일 경로 (/etc/nginx/conf.d/default.conf)로 복사
COPY nginx-test.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

# Nginx가 포어그라운드에서 실행되도록 설정하여 Docker 컨테이너가 종료되지 않게
CMD ["nginx", "-g", "daemon off;"]